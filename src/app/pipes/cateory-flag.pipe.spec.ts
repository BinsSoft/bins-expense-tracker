import { CateoryFlagPipe } from './cateory-flag.pipe';

describe('CateoryFlagPipe', () => {
  it('create an instance', () => {
    const pipe = new CateoryFlagPipe();
    expect(pipe).toBeTruthy();
  });
});
